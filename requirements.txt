# ── Dataset e geração sintética ────────────────────────────────────────────
diagrams==0.23.4          # geração de diagramas cloud (AWS/Azure/GCP)
Pillow==10.3.0            # manipulação de imagens
huggingface_hub==0.23.0   # download de datasets do Hugging Face

# ── SLM / Detecção de objetos (YOLOv8) ─────────────────────────────────────
# Pivot Florence-2 → YOLOv8 (ver PLANO_SPRINT_FINAL.md).
ultralytics==8.2.28       # YOLOv8 — inferência do SLM em api/main.py e slm/predict.py
torch==2.3.0               # dependência do ultralytics (CPU por padrão; ver notebook de treino p/ GPU)

# ── APIs LLM ───────────────────────────────────────────────────────────────
anthropic==0.28.0         # Claude API
openai==1.30.1            # OpenAI API (GPT-4o)

# Base de contramedidas STRIDE: KB estática em stride/analyzer.py (pivot do
# plano original de RAG/LangChain+ChromaDB — ver PLANO_SPRINT_FINAL.md).

# ── API Backend ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9   # upload de arquivos no FastAPI

# ── Interface ──────────────────────────────────────────────────────────────
streamlit==1.35.0
pandas==2.2.2              # tabela de ameaças em app/streamlit_app.py
requests==2.32.3           # app/streamlit_app.py como client HTTP da api/main.py

# ── Geração de relatório PDF ───────────────────────────────────────────────
weasyprint==62.3
jinja2==3.1.4             # templates HTML

# ── Utilitários ────────────────────────────────────────────────────────────
python-dotenv==1.0.1      # carregar variáveis do .env
pydantic==2.7.1           # validação de dados (usado pelo FastAPI)
tqdm==4.66.4              # barras de progresso
numpy==1.26.4
opencv-python==4.9.0.80   # manipulação de imagens para bounding boxes
